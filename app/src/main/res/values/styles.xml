<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>



    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="RegularPopup" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:itemBackground">@color/colorPopup</item>
    </style>

    <style name="WhitePopup" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:itemBackground">@color/colorPopup</item>
    </style>

    <style name="RedPopup" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/colorRed</item>
        <item name="android:itemBackground">@color/colorPopup</item>
    </style>
</resources>
